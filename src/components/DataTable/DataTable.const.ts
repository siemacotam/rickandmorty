import { Checkbox, styled } from "@mui/material";

export const headCells = ["Name", "Avatar", "Origin", "Gender", "Status"];

export const StyledCheckbox = styled(Checkbox)`
  color: rgba(186, 198, 216, 1);
`;
